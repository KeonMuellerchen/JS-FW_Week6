extends ../layout

block content
.container
    h1 All Projects

    if isAuthenticated
        a(href="/projects/new") Create Project

    ul
        each project in projects
            li
                a(hreg=`/projects/${project._id}`)= project.title